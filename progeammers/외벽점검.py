n = 12
weak = [1, 5, 6, 10]
dist =	[1, 2, 3, 4]	
from itertools import permutations

def visited():
    visited = [0] * len(weak)
    start = weak[0]
    for i in range(len(group)):
        if 

        

for i in range(len(weak)):
    first = weak[0]
    weak = weak[1:]
    weak.append(first+12)
    print(weak)

